# Production Profile Configuration
# CORS: Allow studio production domain
# TODO: Replace with actual production studio domain when available
# Example: quarkus.http.cors.enabled=true
# Example: quarkus.http.cors.origins=https://studio.aurenworks.com
# 
# Security: CORS is explicitly disabled until origins are configured.
# This ensures CORS is only enabled for defined origins per security rules.
quarkus.http.cors.enabled=false

